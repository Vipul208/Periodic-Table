import 'package:flutter/material.dart';

class absolute_entropy extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Absolute Entropy"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Absolute Entropy (of a substance)
                
 The increase in the entropy of a substance
 as it goes from a perfectly orderded
 crystelline from 0K(where its entropy is
 zero) to the temperature in the question.""",
                  style: TextStyle(
                    color: Colors.white,
                    fontSize: 17,fontWeight: FontWeight.w500
                ),),
              ),
            )
          ],
        )
    );
  }
}
class absolute_zero extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Absolute Zero"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Absolute Zero
                
 The zero point on the absolute temperature
 scale; -273.12°C or 0K; theoretically, the
 temperature at which molecular motion
 ceases.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class base extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Base"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Base
                
 A substance that produce OH(aq) ions in
 aqueous solution. Strong soluble bases
 are soluble in water and are completely
 dissociated. Weak bases ionize only slightly.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class buffer_solution extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Buffer Solution"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Buffer Solution
                
 Solution that resists change in pH;
 contains either a week acid and a 
 soluble ionic salt of the acid or
 a weak base and a soluble ionic
 salt of the base""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class chemical_bonds extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Chemical Bonds"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Chemical Bonds
                
 The attractive forces that hold atoms
 together in elements or compounds.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class chemical_equations extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Chemical Equations"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Chemical Equations
                
 Description of a chemical reaction by placing
 the formulas of the reactants on the left and
 the formulas of products on the right of an 
 arColumn.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class coordination_number extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Coordination Number"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Coordination Number
                
 In describing crystals, the number of 
 nearest neighbours of an atom or icon.
 The number of donar atoms coordinated
  to a metal""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class coordination_sphere extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Coordination Sphere"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Coordination Sphere
                
 The metal ions and its coordinating ligands
 but not any uncoordinated counter-ions.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class dilution extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Dilution"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Dilution
                
 Process of reducing the concentration of a
 solute in solution, usually simply by mixing
 with more solvent.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class dipole_moment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Dipole Moment"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Dipole Moment
                
 The product of the distance separating 
 opposite charges of equal magnitude of the
 charge; a measure of the polarity of a bond
 or molecule; a measured dipole moment
 refers to the dipole moment of an entire
 molecule.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class distillation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Distillation"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Distillation
                
 The separation of a liquid mixture into its
 components on the basis of differences
 in boiling points. The process in which
 components of a mixture are separated by
 boiling away the more volitile liquid.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class electrochemistry extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Electrochemistry"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Electrochemistry
                
 Study of cheical changes produced by
 electrical current and production of
 electricity by chemical reactions.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class electromagnetic_radiation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Electromagnetic Radiation"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Electromagnetic Radiation
                
 Energy that propagated by means of 
 electric and magnetic fields that oscillate
 in directions perpendicular to the direction
 of travel of the energy.
 """,
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}class electron_affinity extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Electron Affinity"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text("""Electron Affinity
                
 The amount energy absorbed in the
 process in which an electron is added to a
 neutral isolated gaseous atom to form a
 gaseous ion with a 1-charge; has a negative
 value if energy is released. """,
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class fluids extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Fluids"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Fluids
                
 Substance that flow freely; gases and
 liquids.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class flux extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Flux"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Buffer Solution
                
 A substance added to react with the charge,
 or a product of its reduction, in metallurgy;
 usually added to lower a melting point.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class free_radical extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Free Radical"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Free Radical
                
 A highly reactive chemical species carrying
 no charge and having a single unpaired
 electron in an orbital.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class freezing_point extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Freezing Point Depression"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Freezing Point Depression
                
 The decrease in the freezing point of a 
 solvent caused by the presence of a solute.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class gangue extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Gangue"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Gangue
                
 Sand, rock, and other impurities surrounding
 the mineral of interest in an ore.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class gel extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Gel"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Gel
                
 Colloidal suspension of a solid dispersed in
 a liquid; a semi rigid solid.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class ground_state extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Ground State"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Ground State
                
 The lowest energy state or most stable state
 of an atom, molecule or ion.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}
class heat extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Heat"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Heat
                
 A form of energy that flows between
 two sample of matter because of their
 difference in temperature.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}class heat_capacity extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Heat Capacity"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Heat Capacity
                
 The amount of heat required to raise the
 temperature of a body(of any mass) one
 degree Celsius.""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}class uffer_solution extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Buffer Solution"),
          backgroundColor: Colors.amber[400],
        ),
        body: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          children: <Widget>[
            Padding(padding: EdgeInsets.only(top: 130)),
            Padding(padding: EdgeInsets.all(18)),
            Center(
              child: Container(
                alignment: Alignment.center,
                width: 350,
                height: 250,
                color: Colors.pinkAccent[700],
                child: Text(""" Buffer Solution
                
 Solution that resists change in pH; contains
 either a week acid and a soluble ionic salt of
 the acid or a weak base and a soluble ionic
 salt of the base""",
                  style: TextStyle(
                      color: Colors.white,
                      fontSize: 17,fontWeight: FontWeight.w500
                  ),),
              ),
            )
          ],
        )
    );
  }
}






